# Бэклог

Единое место для идей и записей, которые нужно обработать позже. Корневой
`TODO` **мигрирован сюда целиком и удалён** (2026-09-02): реализованное
отброшено, живое разнесено по разделам ниже, отклонённое зафиксировано с
причиной. Всё, что здесь есть, проверено по текущему коду.

**Статусы:**

- **РЕАЛИЗОВАТЬ** — решено делать, ждёт очереди.
- **ПРОРЕСЁРЧИТЬ** — неизвестно, надо сначала выяснить (часто живой прогон,
  а не TDD).
- **ОТЛОЖЕНО** — осознанно не делаем сейчас, причина указана.
- **ОТКЛОНЕНО** — пробовали, не взлетело; надгробие с причиной, чтобы идею
  не воскрешали вслепую.

**Правила ведения:**

- Каждый пункт — с привязкой `файл:строка` или конкретным артефактом, иначе
  он через месяц не проверяем.
- Перед тем как брать пункт в работу — **сверь с текущим кодом**: этот файл
  датирован моментом записи, репозиторий движется быстрее.
- Закрытые пункты отсюда **удаляются**, а не помечаются — история живёт в
  `CHANGELOG.md` и git. Бэклог показывает только открытое. Исключение —
  раздел ОТКЛОНЕНО: он существует именно затем, чтобы привлекательная на вид
  идея не предлагалась заново каждые полгода.
- Баги, привязанные к конкретному скрипту, живут не здесь, а в его плейбуке
  `docs/prompts/<скрипт>.md` (секция «НАЙДЕНО АУДИТОМ …») — так их
  подхватывает сессия по этому скрипту. Сюда попадает кросс-скриптовое,
  инфраструктурное и идеи.

Сверено с кодом: **2026-09-09** (VERSION 1.16.1). Обновление после второго
полного аудита (2026-09-04, 40+ агентов, report-only): 6 прежних High-фиксов
подтверждены повторно; добавлены новые открытые находки (B-6…B-10), поправлен
неверно закрытый R-3.

---

## РЕАЛИЗОВАТЬ

### B-1 · [High] Инкрементальный персист в free_games

`app/free_games/orchestrate.py:92` — `licenses.add_licenses(client, pending)`
возвращает результат целиком, и только потом (строки 94–99) идёт
`mark_added/refused/error`. Сигнатура `add_licenses(client, appids, *,
batch_size)` (`licenses.py:155`) не принимает `persist`-колбэк.

Ctrl+C или исключение во время многоминутного retry-шторма → уже выданные
Steam лицензии не попадают в `added.txt`, следующий прогон тратит на них
квоту повторно (а потолок бесплатных лицензий конечен).

Готовый образец фикса — `app/wishlist/wishlist_api.py::add_pending(persist=…)`,
где этот же класс бага закрыт живой находкой 2026-07-19.

> Единственная **незакрытая** High-находка аудита 2026-08-10 — остальные шесть
> закрыты в v1.15.x–v1.16.1. Пока не починено — не прерывать `--add` на
> полпути, гонять порциями через `--limit`.

**Готовый TDD-фикс уже существует и лежит без дела**: локальная ветка
`recovered/fg1-persist-callback` (persist-callback ровно по образцу
`wishlist_api.py::add_pending`, + тесты на `licenses.py`/`orchestrate.py` —
`git diff develop...recovered/fg1-persist-callback --stat`) восстановлена из
stash параллельной сессии, **не запушена на origin** (риск потери при потере
машины/профиля) и не влита в develop. Решение по ней — за пользователем, не
молча мержить (см. память `feedback_parallel_session_boundary`): как минимум
запушить на origin, чтобы снять риск потери; доводить/мержить — отдельное
решение.

### B-2 · [Med] Нет тестов `main()` у обоих library-скриптов

Отсутствуют `tests/unit/test_add_free_main.py` и `test_wishlist_add_main.py`
— у остальных трёх скриптов такие есть (`test_boost_main.py`,
`test_farm_main.py`, `test_cards_farm_main.py`). Непокрыты: разбор флагов,
порядок resolve→validate, guard `--list` vs `--reset`/`--retry-errors`,
wiring в `run()`. Образец для копирования — `test_boost_main.py`.

Плюс `tests/unit/test_ground_truth.py` (v1.16.0) кроет только чистый
success/partial-путь — не неоднозначный случай «`game_count>0`, `games=[]`»
(см. B-6), из-за которого сам факт этого теста ложно выглядит достаточным.

### B-6 · [High] Неоднозначный пустой ответ Steam API неотличим от потери данных

`app/steam/steam_api.py:143` (`fetch_owned_games`) с v1.16.x логирует
warning, различающий «аккаунт реально пуст» (`game_count=0`) от «Steam
отдал `game_count>0` с пустым `games`» (частичный/битый ответ) — но
**контракт возврата не изменился**, в обоих случаях по-прежнему `[]`.
Вызывающие не могут отличить эти случаи программно, только по логу.

Прямое следствие — `scripts/verify/ground_truth.py::_check_free_games`/
`_check_wishlist`: при таком битом ответе `owned = ∅`, значит
`added - owned` = ВСЕ appid из `added.txt`. Инструмент, чья единственная
задача — ловить реально потерянный персист (B-1), на транзиентном сетевом
сбое сам даёт ложный сигнал «всё потеряно».

Плюс `main()` (`ground_truth.py:74-116`) оборачивает в try/except только
`resolve_steam_id` — вызовы `_check_free_games`/`_check_wishlist` ничем не
защищены: `RuntimeError` из `_api_get` уронит сырой traceback вместо
честного лога и `exit(1)`.

Фикс: либо `fetch_owned_games` поднимает исключение на битом ответе вместо
`[]`, либо возвращает сигнал «ambiguous» отдельно от «genuinely empty»;
`ground_truth.py::main()` оборачивает оба чек-вызова в try/except.

### B-7 · [High] Telegram-токен всё ещё может утечь в логи — `.strip()` неполный

`app/config.py:161-165` — `.strip()` убирает только края строки. Управляющий
символ ВНУТРИ токена (например, случайно вставленный `\n`/`\t` при
копипасте) `.strip()` из середины не убирает. `send_telegram`
(`app/notify.py:82`) собирает URL f-строкой с токеном внутри — такой символ
роняет `http.client.InvalidURL`, чей `__str__` включает URL **целиком**
(токен в cleartext), и это уходит в `log.warning`, живущий в `logs/`
бессрочно plaintext (см. B-3).

Комментарий в самом коде (`config.py:161-163`) описывает именно эту угрозу
— видно, что фикс v1.15.3 целился в неё, но закрыл только внешнюю порчу
строки, не внутреннюю. Фикс: заменить/дополнить `.strip()` валидацией
формата (например, `re.fullmatch` на ожидаемый паттерн токена/chat_id,
reject при несовпадении) вместо частичной очистки.

### B-8 · [Med] `SetThreadExecutionState` без проверки результата

`app/sam/launcher.py:230` (`prevent_idle_sleep`) — ctypes-вызов без
`.argtypes`/`.restype` и без проверки возвращаемого значения, в отличие от
установленного в проекте паттерна (`app/sam/win32_utils.py:24-30`). Молчаливый
сбой (возврат 0) неотличим от успеха — если авто-сон всё же наступит посреди
многочасового прогона, лог не покажет причину.

### B-9 · [Low] `prevent_idle_sleep()` вне try/finally, охраняющего picker

`scripts/achievements/farm.py:380` — вызов стоит МЕЖДУ `launch_picker()`
(строка 369) и `try/finally` (строка ~388), который гарантирует
`_teardown_picker(proc)`. Если `prevent_idle_sleep()` бросит исключение, уже
запущенный `SAM.Picker.exe` останется висеть — process leak.

### B-10 · [Low] README не документирует `scripts/verify/ground_truth.py`

Инструмент v1.16.0, живой и полезный (сверка `added.txt` с реальным
состоянием Steam) — отсутствует и в дереве Project Structure, и в разделе
Usage `README.md`. Отдельно от B-5 (там про дефолт `sam_game_exe_path`).

### B-11 · [Med] Отдельный скрипт для добавления demo-игр

Сейчас добавление demo — только побочный эффект `add_free.py`
(`discovery.discover_candidates(include_demos=True)`, `category1=10`) вместе
с F2P-играми и бесплатными приложениями, в общий `added.txt`. Решено: нужен
отдельный `scripts/library/add_demos.py`, который добавляет ТОЛЬКО демо, не
трогая остальные два источника.

Инфраструктура уже есть и переиспользуема: `discovery._collect_category`
(отфильтровать на `category1=_CATEGORY_DEMOS`), `licenses.add_licenses` /
`request_free_license` (тот же лицензионный путь — демо добавляются точно
так же, как F2P), `app/free_games/orchestrate.py::run` как образец
оркестрации (сейчас жёстко завязан на `discover()` с флагом `include_demos`,
а не на «только демо» — понадобится вариант без остальных двух источников).

Открытые вопросы дизайна (решить перед TDD, не по ходу):

- Отдельный state-файл (`added_demos.txt`) или общий `added.txt` с
  `add_free.py`? Сейчас демо и так пишутся туда же, дублирования не будет —
  но тогда bookkeeping не совсем «отдельный».
- `add_free.py --no-demos`/`include_demos` — оставить как fallback для тех,
  кто хочет всё одним прогоном, или демо целиком переезжают в новый скрипт?
- `ground_truth.py::_check_free_games` сверяет `added.txt` с
  `GetOwnedGames` одним способом для игр и демо — если state разъедется по
  файлам, сверку тоже придётся разделить.

### B-3 · [Med] Логи растут без ротации и удержания

`app/logging_setup.py` создаёт новый `logs/<category>/TIMESTAMP.log` на
каждый запуск — навсегда, без лимита, без очистки. Инструмент гоняется
помногу раз в день месяцами (сессии с ~2026-03), пять категорий подкаталогов.

Помимо самого исчерпания диска: на полном диске тихо ломается
`_atomic_write_text` (tempfile + `os.replace`), а на нём завязаны ВСЕ
state-файлы прогресса. Ни один сценарий целостности данных этого не учитывал.

> Сюда же поглощён пункт «Просмотреть логирование скриптов» из корневого
> `TODO`: формат и раскладка логов давно стандартизированы (конвенции в
> CLAUDE.md), непокрытыми оставались ровно две вещи — удержание (этот пункт)
> и санитайзер секретов (R-3).

### B-4 · [Med] `enforce_admins: false` на защите main

Живая проверка `gh api repos/erdogan-deniz/sam-automation/branches/main/protection`
→ `enforce_admins.enabled = false`, ruleset'ов нет, у аккаунта `admin: true`.
Required status checks и required PR **не применяются** к админ-аккаунту:
инвариант «прямой push в main заблокирован» держится только harness-слоем
агента, не самим GitHub.

Либо включить (`gh api -X POST .../protection/enforce_admins`), либо честно
переписать формулировку «push защищён» в `docs/gitflow.md:7` и CLAUDE.md.

### B-5 · [Low] README-дрейф: дефолт `sam_game_exe_path`

`README.md:118` документирует дефолт `./external/SAM/SAM.Game.exe`, но
дефолт датакласса — пустая строка (`app/config.py:58`); задокументированное
значение приходит только из `config.example.yaml`. Если пользователь удалит
строку из своего `config.yaml`, `Path("").exists()` вернёт `True` и
авто-скачивание SAM тихо не сработает. Либо поправить дефолт в коде, либо
уточнить формулировку в README.

---

## ПРОРЕСЁРЧИТЬ

### R-1 · Family-Share невидим для `GetOwnedGames`

`boost.py` меряет прогресс по `playtime_forever` из `GetOwnedGames`, а тот
слеп к играм Family Share. Именно на этом однажды сгорело публичное
заявление про 9416 набитых игр (99% оказались Family Share).

Гипотеза: `GetRecentlyPlayedGames` их видит. Надо выяснить живьём, что
реально отдаёт API для Family-Share игр и можно ли на этом строить
измерение. До этого — считать метрики boost по Family-Share недостоверными.

### R-2 · Zero-transition flush в cards-фарме не подтверждён

`_FLUSH_PAUSE_SECONDS = 20` в `scripts/cards/farm.py` — неизмеренная
константа. Механика «коллапс всех активных игр в ноль выбивает накопленные
карты» задокументирована по Idle Master Fast mode + наблюдению пользователя,
но **никогда не проверялась e2e**. Если 20 секунд мало — дроп просто
увидится следующим циклом (не потеря), так что риск низкий, но и уверенности
нет. Это живой прогон, не TDD.

### R-3 · Нужен ли системный санитайзер секретов в логах

`grep -rn "redact|sanitiz|_mask" app/` — пусто, общего хелпера нет. При этом
`log.error("…: %s", e)` встречается рядом с cookie/JWT/password-кодом, а логи
лежат plaintext вечно (см. B-3) и часть содержимого уходит в Telegram.

Один реальный лик уже нашли (токен Telegram попадал в `InvalidURL.__str__`,
v1.15.3), но фикс оказался **неполным** — см. B-7: `.strip()` закрыл только
внешнюю порчу строки, не внутренний control-символ. Прочие secret-adjacent
пути проверялись точечно и на тот момент были чисты. Вопрос: строить ли
системный санитайзер на уровне `logging_setup`/`notify`, или точечных
проверок (вроде B-7) достаточно — зависит от того, насколько дорого
поддерживать первое.

### R-4 · Живучесть CM-сессии в многочасовых прогонах

Частично закрыто в v1.15.4 (честный abort вместо бесконечного hang на
мёртвой CM-сессии — но только в `free_games`). Не проверялось для
`boost.py`/`achievements/farm.py`: что произойдёт, если аккаунт залогинится
с другого устройства посреди многочасового цикла и сервер отзовёт сессию —
честный abort, тихий hang, или волна ложных per-game ошибок, которая
затриггерит `max_consecutive_errors`.

### R-5 · UIA при блокировке экрана / дисконнекте RDP

Частично закрыто в v1.16.1 (предотвращение авто-сна по простою). Остаётся
открытым ручной сценарий: Win+L или обрыв RDP посреди прогона. Windows UI
Automation деградирует на заблокированном рабочем столе — вопрос, отличит ли
код «среда сломалась» от волны per-game ошибок. Если не отличит, блокировка
экрана может завалить батч и, хуже, наплодить ложных записей в `error.txt`.

### R-6 · Бесплатный DLC для уже имеющихся игр

Из корневого `TODO` («Добавление в библиотеку бесплатного дополнительного
контента для уже имеющихся игр») — вместе с D-5 единственные два пункта
оттуда, которые ещё не реализованы и не потеряли смысл. `add_free.py`
добавляет бесплатные
игры/приложения; отдельный вопрос — работает ли `request_free_license` так
же для DLC, и как их вообще обнаруживать (store-search по категории DLC +
фильтр «владею базовой игрой»). Сначала ресёрч, потом решение.

---

## ОТЛОЖЕНО

### D-1 · Вынести оркестрацию из толстых scripts в `app/`

`scan.py` 239 · `achievements/farm.py` 422 · `cards/farm.py` 368 ·
`playtime/boost.py` 441 строк бизнес-логики живут в `scripts/`, то есть **вне
гейта `mypy app`** (он scoped только на `app/`). Образец, как надо, уже есть:
`app/free_games/orchestrate.py` и `app/wishlist/orchestrate.py` — скрипт
зовёт один `run(...)`.

**Отложено**: это money-path, риск регресса выше пользы от типизации. Браться
только с отдельным дизайном и полным TDD, не «между делом».

### D-2 · `get_web_cookies` торчит из двух публичных путей

`app/steam/steam_cm.py:191` реэкспортирует `app.cookies.get_web_cookies`,
`app/steam/__init__.py:4,11` кладёт его в `__all__`. Вызывающие расходятся:
`scripts/cards/farm.py` берёт через `app.steam`, `app/wishlist/*` — напрямую
из `app.cookies`. **Отложено**: чистая косметика, ломать импорты ради неё
сейчас незачем.

### D-3 · `app/cookies/__init__.py` — гибридный фасад

Вся 4-шаговая fallback-цепочка `get_web_cookies` реализована прямо в
`__init__.py` (строка 24), в отличие от чистых re-export фасадов
`auth`/`cards`/`sam`. **Отложено** по той же причине, что D-2; логично делать
одним заходом с ним.

### D-4 · `app.steam` не листовой

`import app.steam` транзитивно тянет `app.auth.*` и `app.cookies.*` (живой
sys.modules-диф), даже если вызывающему нужен только
`steam_local`/`steam_registry`. Утяжеляет граф импорта. **Отложено**:
измеримого вреда (времени старта) не замерялось — если браться, сначала
замер, потом решение.

### D-5 · Группировка игр в библиотеке

Из корневого `TODO`, раздел «На подумать». Идея без сформулированной задачи:
неясно, что именно группировать (Steam-категории? свои коллекции через какой
API?) и зачем. **Отложено** до появления конкретной боли.

---

## ОТКЛОНЕНО

> Идеи, которые уже пробовали реализовать и осознанно свернули. Выглядят
> привлекательно и будут предлагаться снова — здесь причина, почему не надо.

### X-1 · Раскладывать игры по спискам «с достижениями / без» на этапе scan

Из корневого `TODO` («Scan должен показывать все игры с достижениями»,
«хочу, чтобы scan раскидывал все игры по файлам соответствующим»).

**Пробовали дважды и оба раза откатили:**

1. v1.1.0 — каталог `with`/`without` по Store API. Откачено уже в v1.1.1:
   Store API ненадёжен и **терял игры, у которых достижения есть**. Для
   money-path это худший из возможных отказов — игра молча не обрабатывается.
2. v1.4.0–v1.5.0 — переделано в advisory-каталог (подсказка порядка, не
   фильтр). Удалено целиком в v1.9.x вместе с `app/catalog.py`,
   `scripts/categorize.py`, `app/steam/store_api.py` — фарм и так проходит
   всю библиотеку, а ненадёжные догадки Store API только вводили в
   заблуждение.

**Действующий инвариант** (CLAUDE.md): пометить игру «без достижений»
(`without.txt`) вправе **только SAM**. Никакие внешние источники — Store API,
эвристики, кэши — не имеют права хоронить игру. Если статус неясен, игра
доезжает до SAM.

Возвращаться только с источником, который доказуемо не даёт ложных
негативов — Store API таким не является.

### X-2 · `view` / статистика по играм с достижениями

Из корневого `TODO` («А view? Должен показывать статистику по всем играм с
достижениями + общую статистику»).

Было реализовано (`scripts/stats.py` + `app/stats.py`) и **удалено в
v1.9.x** вместе с GUI-подсистемой при переходе на CLI-only. Статистика не
использовалась в реальной работе, а поддерживать её (кэш имён, обход API,
согласованность с state-файлами) стоило дороже пользы.

Если понадобится снова — это уже другая задача: не воскрешать удалённый
модуль, а решить, какой именно вопрос статистика должна отвечать. Ближайший
живой аналог для сверки с реальностью — `scripts/verify/ground_truth.py`
(v1.16.0).
