# Changelog

Все значимые изменения проекта. Формат — по [semver](https://semver.org).

## [1.15.0]

### Хардненинг после full-project-аудита (2026-08-10)

Отдельная сессия провела 46-агентный security/correctness-аудит (12 финдеров
+ верификация + критик слепых зон) по всем модулям — найдено 7 High
(data-loss/resource-leak/run-lock). Все переоткрыты, адверсариально
верифицированы и исправлены TDD этим релизом:

- **`app/auth/credentials.py`**: легаси-миграция plaintext-JSON → keyring
  теряла единственную сохранённую копию пароля при сбое `_save_session`
  (keyring/DPAPI недоступен) — файл удалялся безусловно, даже без
  подтверждённого успеха миграции. `_save_session` оставляла username-файл
  без пароля при сбое `keyring.set_password` (рассинхрон). `_load_session`
  молча трактовала любой сбой keyring как «пароль не сохранён» —
  добавлено логирование. `_clear_session` ловила только один подкласс
  `KeyringError` (`PasswordDeleteError`), сиблинги (`KeyringLocked` и т.п.)
  пролетали непойманными.
- **`app/cookies/playwright.py`**: `_try_save_cm_refresh_token` кэшировала
  CM JWT-токен в web-scope файл вместо client-scope — токен физически
  никогда не использовался `_cm_login`. На успешном пути `_playwright_login`
  вызывала `browser.close()` ДО сохранения cookie — сбой close() терял уже
  добытый JWT. Незащищённый `_try_save_cm_refresh_token()` мог утопить уже
  успешный основной cookie. Обрыв соединения во время ожидания входа давал
  ту же строку лога, что настоящий таймаут.
- **`app/cookies/storage.py`**: `_save_manual_cookie`/`_save_remember_login`
  писали через сырой `Path.write_text` — переведены на атомарную запись
  (`_atomic_write_text`), как `id_file`/`cache`.
- **`app/sam/picker_session.py`**: cleanup осиротевшего SAM.Game.exe висел
  на узком `except SAMGameError` — не ловил сбои `Application(...).connect()`
  иного рода. Заменён на `try/finally`. Добавлен grace-check на PID,
  появившийся чуть позже основного poll-дедлайна.
- **`app/run_lock.py`**: `_own_token` могла записать литеральную строку
  `"None"` при недоступном `create_time` — другой инстанс счёл бы живого
  владельца мёртвым и снёс его лок. Теперь падает громко вместо записи
  неразрешимого токена. Добавлен reentrancy-guard для повторного захвата
  из того же процесса.
- **`scripts/achievements/farm.py`**: в 3 error-путях `_process_one_game`
  `tracker.record_error()` (может бросить `SAMTooManyErrors` на лимите)
  вызывался ДО `mark_error_id`/`results.append` — бросок пропускал персист,
  игра молча ретраилась бы без следа о прежней ошибке. Порядок переставлен.
  `finally: kill_process(proc)` в `main()` не был защищён — вынесен в
  `_teardown_picker` (никогда не бросает, аналог `boost.py::_teardown`).
- Устранён дрейф документации/мёртвый код, накопившийся с v1.11.0–v1.14.2:
  собственная эталонная структура `docs/prompts/project-auditor.md`
  (пропущены `free_games`/`wishlist`, ложная запись про
  `[tool.pytest.ini_options]`), `config.example.yaml` (неверное описание
  `game_ids`/`game_ids_file`), `docs/gitflow.md` (неверный merge-back флоу),
  `CLAUDE.md`, устаревший `--help` в `farm.py`; мёртвые поля/свойства
  (`UnlockResult.already_unlocked`, `ErrorTracker.total_errors` — теперь
  подключено вместо дублирующего счётчика в `farm.py`), лишние реэкспорты
  `AddResult`; мёртвые записи `.gitignore` (`.cache/`, `Options`).

## [1.14.2]

Найдено систематическим прогоном по стабильности всех скриптов проекта:
флагманский `achievements/farm.py` и `cards/farm.py` — единственные два из
пяти SAM/library-скриптов с багом resolve-before-validate (RA-B), который
уже был закрыт в остальных трёх. Плюс актуализация handoff-плейбуков.
3 новых теста (634 → 637).

### Исправления

- **achievements/farm: резолвить `steam_id` (vanity/URL → ID64) до
  `validate`** — флагманский скрипт проекта вообще НЕ резолвил vanity-имя/
  URL `steam_id` (документированный README формат — «Steam ID, vanity name,
  or full URL», все три принимаются). `validate()` шлёт `steam_id` в
  `GetPlayerSummaries`, которому нужен числовой ID64; сырой vanity/URL давал
  `players=[]` → ложное «Steam API key is invalid or Steam ID not found» →
  `sys.exit(1)` до запуска чего-либо. `cfg.steam_id` нигде в скрипте, кроме
  `validate()`, не используется — фикс идентичен паттерну, уже закрытому в
  `boost.py`/`scan.py`/`add_free.py`/`wishlist_add.py`.
- **cards/farm: тот же фикс** — резолвил `steam_id`, но ПОСЛЕ `validate()`,
  а не до, с тем же практическим эффектом (ложная ошибка раньше, чем резолв
  вообще случится).

### Документация

- `docs/prompts/achievements-farm.md`, `cards-farm.md` — порядок `main()` в
  описании выровнен под фикс; раздел «ПРОБЕЛЫ В ТЕСТАХ» переписан по факту
  текущего кода (achievements: `_process_one_game` давно покрыт тестами,
  реальный пробел — `main()`-цикл целиком; cards: аудит 2026-07-02
  предшествует удалению GUI-подсистемы и конвенции resolve-до-validate, не
  переповторялся с тех пор).
- `docs/prompts/add-free.md`, `wishlist-add.md` — НОВЫЕ handoff-плейбуки.
  Обе фичи (`scripts/library/add_free.py`, `wishlist_add.py`) выпущены
  (v1.13.0/v1.14.0), но не имели стартового брифа для новой сессии — теперь
  единственные два скрипта проекта явно помечены как никогда не проходившие
  формальный многоосевой аудит (в отличие от `scan.py`/`boost.py`/
  `cards-farm.py`).

### Тесты

- 3 новых (634 → 637): порядок resolve→validate для `achievements/farm.py`
  и `cards/farm.py` (по образцу `test_boost_main.py`) — пустой `steam_id` не
  резолвится, сбой резолва даёт чистый `sys.exit(1)` без захода в `validate`.

## [1.14.1]

Закрытие последнего остатка self-аудита v1.12.4 (полиш: неточный докстринг +
тест-пробелы). 8 новых тестов (623 → 631).

### Исправления

- **boost: `_teardown` и финальный отчёт переживают любой `BaseException`** —
  докстринг `_teardown` обещал «никогда не бросает» (RA-C), но особо
  обрабатывал только `KeyboardInterrupt` (bounded retry ×3). Любой ДРУГОЙ
  `BaseException` (`SystemExit`/`GeneratorExit`) из `kill_process`/
  `kill_all_sam_games` не ловился ни внутренним `except Exception`, ни внешним
  `except KeyboardInterrupt`, и пролетал наружу — гарантия, на которую
  полагается вызывающий `finally`-блок, не держалась. Тем же классом уязвим
  `_report_result`: стоял ПОСЛЕ `finally`, ничем не защищённый — третий
  Ctrl+C ровно во время отчёта пробросил бы исключение наружу в `main()`,
  давая сырой краш вместо уже посчитанного честного статуса. Оба места теперь
  гасят `BaseException` на терминальной границе и логируют, вместо проброса.

### Тесты

- 8 новых (623 → 631): `_teardown` переживает non-KI `BaseException` и не
  крутится вечно на непрерывном Ctrl+C (bounded ×3); `_report_result` не
  роняет `_boost_loop`; run-lock деградирует в честный `RuntimeError` на
  устойчивой (не транзиентной) гонке снятия stale-лока, а не крутится вечно;
  потолки длительностей (`playtime_idle_duration`/`launch_stagger`) запиннены
  точно на границе (`>`, не `>=`).

## [1.14.0]

Новая фича: авто-добавление бесплатных игр Steam в библиотеку — пакет
`app/free_games/` и CLI `scripts/library/add_free.py`. Плюс закрытие остатка
self-аудита v1.12.4 (1 MEDIUM, 2 LOW — каждая по TDD) и документирование
обеих library-фич в README. 58 новых тестов (565 → 623).

### Возможности

- **free-games: авто-добавление бесплатных игр в библиотеку** — две фазы.
  *Discover*: store search по категориям (F2P-игры `998`, бесплатный софт
  `994`, демо `10`) с `maxprice=free` → `candidates.txt`, минус owned
  (развёрнутые через `packageinfo.vdf` лицензии живой CM-сессии) минус
  added/refused. *Add*: `request_free_license` батчами по 20 поверх
  переиспользуемого `cm_session()`. Детект потолка free-лицензий аккаунта
  (`EResult.LimitExceeded`) — прогон честно останавливается, а не долбит стену.
- **steam-cm: извлечён `cm_session()`** — контекст-менеджер с живым
  авторизованным CM-клиентом (гарантированный disconnect), переиспользуемый
  вместо копии логин-логики в каждом потребителе.

### Исправления

- **boost: слепой прогон больше не хоронит игры в `skip.txt`** (MEDIUM,
  зеркало RA-A). В слепом прогоне owned-games пуст → ВСЕ игры unknown →
  `known_ids` пуст, и каждый провал запуска уходил в `skip.txt` через
  `on_failed`. В отличие от `done.txt`, skip жёсткий и не самоисцеляется по
  API: один слепой прогон при массовом провале (Steam не поднялся, сеть легла)
  хоронил бы библиотеку навсегда. Теперь `persist_done` гейтит и skip:
  непроверяемый прогон не персистит ничего.
- **id-file: `_append_id` переживает не-UTF-8 id-файл** (LOW) — `read_text`
  бросает `UnicodeDecodeError`, подкласс `ValueError`, а не `OSError`, поэтому
  битый байт (оборванная запись, правка в другой кодировке) пролетал мимо
  guard'а и ронял прогон сырым трейсбеком. Теперь исход тот же, что при сбое
  ввода-вывода: файл не перезаписан, дозапись пропущена, WARNING в лог.
- **config: strip whitespace у `steam_id` и `steam_api_key`** (LOW) —
  скопированное из браузера значение с хвостовым пробелом или переводом строки
  уезжало прямо в URL Web API и роняло каждый запрос ложной ошибкой
  «невалидный ключ / ID не найден» на верных данных.
- **id-file: ретрай `os.replace` на транзиентный `PermissionError`** — живой
  краш `WinError 5` (Windows sharing-violation) во время batch-записи
  `error.txt` при активном прогоне. 5 попыток с паузой 0.1с; симметрично
  `_read_ids_strict`, которая уже переживала тот же класс сбоя на чтении.
- **free-games: `BATCH_SIZE` 50 → 20** — 50 систематически ловил ложный
  `Timeout` на `send_job_and_wait` CM. `Timeout` с `granted_appids=None`
  ретраится, а не уходит в `error`; `RateLimitExceeded` ретраится терпеливо.

### Документация

- README описывает обе library-фичи: раздел «Growing the library» с флагами
  `add_free.py` и `wishlist_add.py`, dry-run по умолчанию и честная остановка;
  `app/free_games/`, `app/wishlist/`, `scripts/library/` в дереве проекта;
  таблица общего state-layout (candidates/added/refused/error). Ранее ни один
  из двух скриптов в README не упоминался.

### Тесты

- 58 новых (565 → 623): free-games (discovery, resume-состояние, батчи с
  cap-детектом, честный отчёт, оркестрация), `cm_session()`, ретрай
  `os.replace`, плюс по одному провалившемуся-первым тесту на каждую находку
  аудита (слепой skip, не-UTF-8 id-файл, whitespace в config).

## [1.13.0]

Новая фича: авто-добавление каталога Steam в вишлист аккаунта — пакет
`app/wishlist/` и CLI `scripts/library/wishlist_add.py`. Write-путь снят
живьём на реальном аккаунте, не по документации. Плюс ретрай транзиентных
сетевых сбоев в Web API и закрытие E-пробелов покрытия. 108 новых тестов
(457 → 565).

### Возможности

- **wishlist: авто-добавление каталога Steam в вишлист** — две фазы.
  *Discover*: вселенная через `IStoreService/GetAppList/v1` с пагинацией по
  `last_appid` (все типы контента: games/dlc/software/videos/hardware), минус
  owned (`GetOwnedGames`), минус уже-в-вишлисте (`IWishlistService/GetWishlist/v1`,
  keyless), минус added/refused из resume-состояния → `candidates.txt`.
  *Add*: по одному appid (batch-эндпоинта нет) через
  `POST IWishlistService/AddToWishlist/v1` с community-JWT из
  `app.cookies.get_web_cookies`. Легаси store-эндпоинт `/api/addtowishlist`
  отклонён живой проверкой: он требует токен с `aud=web:store`, которого
  `get_web_cookies` не выдаёт.
- **wishlist: классификатор ответа по заголовку `x-eresult`** (authoritative
  WebAPI-сигнал): `1` → added; `2` (owned/уже-в-вишлисте) и `8`
  (delisted/несуществующий) → refused, терминально; `84` либо голый HTTP 429 →
  rate-limit; HTTP 401 → auth-fail.
- **wishlist: адаптивный backoff-then-stop вместо хардкод-троттла** — живая
  мера показала ~2 добавления/сек без единого троттла, устойчивый предел за
  тысячи adds не измерялся намеренно (IP soft-ban ~6ч продлевается при
  долбёжке). Поэтому идём быстро и отступаем ТОЛЬКО по реальному сигналу
  Steam: 60/120/240/300с на растущий streak, 5 подряд rate-limit → стена и
  остановка, оставшийся pending не трогаем.
- **wishlist: resume-состояние** `data/games/ids/wishlist/` — candidates,
  added, refused (терминал), error (транзиент, чистится `--retry-errors`).
  Персист инкрементальный, по каждому решённому appid.
- **wishlist: честный отчёт** — стена по rate-limit, auth-fail и Ctrl+C не
  дают success-тост; dry-run — режим по умолчанию, аккаунт мутируется только
  по явному `--add`. Флаги: `--add`, `--list`, `--reset`, `--retry-errors`,
  `--limit`, `--interval`. Run-lock не берётся: SAM не спавнится (как `scan.py`).

### Исправления

- **steam-api: ретрай транзиентных сетевых сбоев в `_api_get`** — SSL-обрыв и
  таймаут хендшейка (не HTTP-ответ сервера) роняли вызов с первой попытки.
- **wishlist: сбой `GetAppList` больше не стирает накопленный
  `candidates.txt`** — живая находка: транзиентный SSL-обрыв пагинации тихо
  превращался в «0 кандидатов», и `discover()` перезаписывал реальный файл
  (211 032 записи) пустым списком. Теперь исключение пробрасывается,
  `run()` ловит его честным `status="error"` и `save_candidates()` не
  вызывается вовсе.
- **wishlist: инкрементальный персист added/refused/error вместо батча** —
  жёсткий килл процесса посреди многочасового прогона терял весь накопленный
  прогресс: Steam добавление уже принял, локально не осело ничего.
- **wishlist: ретрай транзиентного сетевого сбоя в `wishlist_api._call`** —
  единичные SSL EOF/timeout уходили прямо в `error.txt` без единой повторной
  попытки, в отличие от `steam_api._api_get`.
- **wishlist: streak rate-limit сбрасывается на исключении** — сетевой сбой
  посреди серии не должен доталкивать счётчик до ложной «стены».
- **wishlist: appid из `error` исключены из auth-fail retry** — иначе один
  appid мог попасть и в `error`, и в `added` за один прогон.

### Тесты

- 108 новых (457 → 565): 75 на вишлист (классификатор `x-eresult`, backoff и
  детект стены, resume-состояние, пагинация вселенной, дедуп owned/wishlisted,
  честный отчёт, оркестрация с auth-fail retry), остальные — закрытие
  E-пробелов покрытия (`PickerSession.add_and_open_game`, cookie storage,
  `card_cache`, `web_refresh`) и ретрай в `steam_api`.

## [1.12.4]

Ремедиация глубокого+широкого аудита фичи `playtime/boost.py` (16 подтверждённых
находок из 9 осей, каждая адверсариально верифицирована). Блокеров нет; закрыты
3 MEDIUM, 5 LOW, INFO и тест-пробелы. Инвариант безопасности run-lock (нет
двойного захвата) сохранён. +28 тестов (429 → 457).

### Исправления

- **boost: транзиентно-пустой owned-games больше не травит `done.txt` всей
  библиотеки (RA-A)** — `_select_targets` проверял `done.txt` (в `skip`) до
  гейта played/target, а `done` пишется только для unknown-игр. Пустой
  `GetOwnedGames` (битый/приватный ответ) помечал done всю библиотеку →
  следующий здоровый прогон выдавал «Нет игр для обработки». Теперь `done`
  глушит игру только пока она unknown (known гейтятся по API — самоисцеление
  после сбоя); слепой прогон (`blind`, пустой owned) не персистит done вовсе.
  Инвариант «unverified → НЕ done».
- **boost: `resolve_steam_id` вызывается ДО `validate` (RA-B)** — vanity-имя/URL
  `steam_id` (рекламируемый в `config.example.yaml` формат) падал ложным «API
  key invalid»: `validate` слал сырой id в GetPlayerSummaries (нужен числовой
  ID64) до резолва. Порядок выровнен со `scan.py`.
- **boost: teardown-свип НИКОГДА не бросает (RA-C)** — не-KeyboardInterrupt
  исключение из `kill_process`/`kill_all_sam_games` (на Windows `proc.kill()` →
  PermissionError в гонке терминации) осиротляло SAM.Game.exe и глотало финальный
  отчёт. Вынесен `_teardown`: каждый kill изолирован, бэкстоп и `_report_result`
  отрабатывают всегда; повторный Ctrl+C → bounded-retry.
- **boost: idle-окно на монотонных часах (RA-4)** — скачок настенных `time.time()`
  (NTP-ресинк/возврат из suspend) обрывал idle досрочно → ложный `done` для
  unknown-игры. Перевод на `time.monotonic()`.
- **config: `_num` ловит `OverflowError` (RA-5)** — `<int-поле>: .inf` роняло
  `load_config` сырым трейсбеком до валидации (`int(float('inf'))`). Теперь
  дефолт + warning, как для прочих нечисловых.
- **validator: не-конечный `launch_stagger` отбраковывается (RA-6)** — `.nan/.inf`
  проскакивали guard `>= 0` (`nan < 0` == False) → `time.sleep(nan)` крашил
  батч. Проверка через `math.isfinite`. + щедрые потолки длительностей
  (idle ≤ 86400с, stagger ≤ 3600с) ловят типо-валидную опечатку.
- **run-lock: ретрай захвата до дедлайна (RA-7)** — жёсткий `range(2)` давал
  ложный отказ старта на СВОБОДНОМ (мёртвом) локе при контеншене за снятие
  stale-токена (проглоченный Windows sharing-violation на unlink). Ретрай до
  дедлайна; safety-инвариант (O_EXCL + compare-and-delete, не клоббер живого
  чужого лока) не тронут.
- **id-file: `_append_id` не усекает файл при сбое чтения (RA-8)** — транзиентный
  сбой чтения существующего id-файла (Windows sharing-violation) перезаписывал
  его одним новым id, теряя всё накопленное. Строгое чтение для писателя; при
  сбое дозапись пропускается, файл цел (для resume-файлов безвредно).
- **id-file: `read_ids_ordered` дедупит `all.txt`** — дубль appid давал двойной
  запуск игры (два SAM.Game.exe на один appid дерутся за global user).

### Тесты

- +28 (429 → 457): гейт-порядок done/known, blind-флаг, present-owned склейка на
  ненулевом playtime, оркестрация `main` (resolve→validate, пустой/сбойный id),
  не-KI-в-свипе, монотонные часы, прямая матрица `_report_result` (мутационно
  проверена), OverflowError/nan/inf/потолки длительностей, run-lock liveness
  (+ сохранён safety-тест не-клоббера), read-failure сохраняет накопленное, дедуп.

## [1.12.3]

Функциональное покрытие конечного автомата Steam CM. Изменений поведения нет —
только тесты. +5 тестов (424 → 429).

### Тесты

- Сквозные тесты `read_steam_cm_app_ids` на фейковом SteamClient (без сети и
  реального логина): JWT-первый успех, вход по сохранённому паролю, RSA-успех
  (legacy InvalidPassword → RSA OK), транзиент → пропуск CM с сохранением кред
  (инвариант «transient не стирает креды» на уровне всей функции), 2FA с
  авто-TOTP. Закрыт остаточный пробел покрытия, отмеченный ещё исходным
  CM-аудитом (сама функция покрывалась функционально лишь одним путём).

## [1.12.2]

Закрытие остатка мета-аудита v1.12.0: добавлен последний отложенный
интеграционный тест и исправлен косметический недосчёт в логе `packageinfo`.
Открытых находок аудита больше нет. +2 теста (422 → 424).

### Исправления

- **packageinfo: счётчик найденных пакетов** — `found_pkgs++` перенесён на
  конец успешного разбора пакета; битый владеемый пакет больше не числится
  ложно найденным (лог «пропущено пакетов» теперь корректен).

### Тесты

- +2: интеграционный тест шва `read_steam_cm_app_ids` (достоверный
  InvalidPassword → `_clear_session` → пропуск CM при `interactive=False`) —
  закрыт последний отложенный пробел покрытия; корректность счётчика
  пропущенных пакетов в `packageinfo`.

## [1.12.1]

Правки по мета-аудиту v1.12.0 (7 независимых аудиторов): устранение
честностных оверклеймов отчётности и защитных LOW-находок. Блокеров, регрессий
у потребителей и потери кред/зависаний аудит не выявил; инвариант «transient не
стирает креды» подтверждён. +9 юнит-тестов (413 → 422).

### Исправления

- **scan: `resolve_steam_id` усилен** — пустой `steam_id` больше НЕ резолвится
  (был лишний сетевой вызов вместо локальной ошибки validate); `except` ловит и
  `KeyError` (аномальный ответ ResolveVanityURL) → чистый exit(1), не трейсбек.
- **steam_registry: `steamid64_to_id3` → `isdecimal`** (был `isdigit`) —
  юникод-«цифра» давала сырой ValueError вместо SAMError (тот же класс, что
  закрыт в VDF-парсере v1.12.0, но в реестре пропущенный).
- **CM: `_clear_session` сносит и JWT-кэши** — после стирания на достоверно-
  неверном пароле short-circuit `_jwt_web_cookies` (игнорирует username) иначе
  переиспользовал бы старый client-scope токен для другого аккаунта.
- **CM: `rsa_tried` сбрасывается на новом пароле** — исправленный пароль
  modern-auth аккаунта после опечатки иначе не получал RSA и ложно уходил в
  InvalidPassword.
- **packageinfo: внешний try вокруг цикла** — повреждение на уровне ПОТОКА (не
  отдельного пакета) больше не теряет ВСЕ CM-игры (возвращается распарсенное).

### Документация

- **scan.md playbook** приведён к коду v1.12.0: флаг `--allow-shrink`,
  floor-guard + атомарная запись в описании main(), quote-aware токенайзер
  вместо «регэксп + ручной баланс», существование `test_scan.py`, обновлённая
  секция РИСКИ.
- **CHANGELOG 1.12.0** (VDF): «верифицировано не-регрессивно на реальном файле»
  → честно «синтетические vdf-фикстуры; реальный localconfig.vdf в автотесты не
  входит» (ручной smoke невоспроизводим из репо).

### Известный остаток (не блокер)

- Интеграционный тест шва «достоверный InvalidPassword → `_clear_session` →
  ре-промпт» в `read_steam_cm_app_ids` не добавлен (требует мока ~15
  коллабораторов; компоненты покрыты юнит-тестами). Прочие LOW приняты by
  design: floor-guard ловит только усадку >50%; счётчик пропущенных пакетов в
  packageinfo — косметика лога.

## [1.12.0]

Аудит `scan.py` (вглубь и вширь) и устранение находок: целостность мастер-файла
`all.txt`, надёжность трёх источников (VDF / Web API / CM), восстановление CM,
чистка дрейфа. TDD по каждому фиксу; +57 юнит-тестов (356 → 413), 4 гейта
зелёные. Изменения проведены изолированными feature-ветками с adversarial-review
полного диффа перед релизом.

### Исправления — целостность all.txt

- **Атомарная запись `all.txt`** (`scan.py`): мастер-файл писался прямым
  `write_text` в обход проектного `_atomic_write_text` — kill/Ctrl+C/полный диск
  в момент записи усекал список. Переведено на атомарную запись; заодно устранён
  `mkdir` без `parents=True`.
- **Floor-guard против тихой усадки**: транзиентный отказ API/CM мог схлопнуть
  `all.txt` (напр. до VDF-only). Усадка >50% относительно прежнего размера
  блокирует перезапись (честный лог + `exit 1`); обход — флаг `--allow-shrink`.

### Исправления — источники

- **Steam Web API**: одна запись без `appid` роняла ВЕСЬ owned-games (KeyError) —
  отфильтровано; сбой сохранения `names.json` развязан и больше не отбрасывает
  список ID; добавлен ограниченный ретрай на 429 (учёт `Retry-After`, потолок).
- **VDF-парсер переписан** (`steam_local`): quote-aware токенайзер + стековый
  walker с depth=1 — скобки внутри значений (LaunchOptions) не рвут блок,
  вложенные numeric-ключи (`depots`) не дают фантомных App ID, `apps` ищется как
  прямой ребёнок Steam (не обязана быть первой), дедуп, distinct-warning на
  повреждённом файле; ключи проверяются `isdecimal` (юникод-«цифра» не роняет
  чтение всей библиотеки). Покрыто синтетическими vdf-фикстурами по классам
  багов; реальный localconfig.vdf в автотесты не входит.
- **vanity/URL `steam_id`**: scan теперь резолвит через `resolve_steam_id` до
  валидации (как boost/cards) — прежде vanity/URL падал с вводящей в заблуждение
  ошибкой «API key invalid».
- **Реестр**: `steamid64_to_id3` валидирует вход (`SAMError` вместо сырого
  `ValueError`/отрицательного пути); `find_steam_path` терпит пустой
  `InstallPath` и не-`FileNotFound` `OSError` (продолжает к следующему hive).

### Исправления — Steam CM

- **Устранён вечный цикл интерактивного входа**: `TryAnotherCM`/
  `ServiceUnavailable` крутили reconnect без ре-логина (hard-hang) — добавлен
  ре-логин + кап транзиентных попыток; починен yes/no-гейт (подстрочная
  проверка → явные множества, «no» реально выходит).
- **Восстановление после смены пароля**: достоверно-неверный пароль (отказ
  современного Begin-пути) теперь стирает протухшую сессию и переспрашивает
  логин. Инвариант «transient не удаляет креды» сохранён и энфорсится by
  construction (сигнал узкий; token-этап не даёт ложного вердикта).
- **Прочая устойчивость**: авто-2FA откатывается на ручной ввод при неверном
  авто-коде; один битый пакет в `packageinfo` не роняет весь разбор;
  `read_steam_cm_app_ids` закрывает соединение через try/finally (нет лика в
  cron без stdin); протухший client-scope `refresh_token` чистится.

### Чистка

- Удалена мёртвая зависимость `cryptography` (потребители chrome/dpapi вырезаны
  в v1.11.0); стале-комментарий playwright актуализирован.
- Убраны стале psutil/Steam-process тесты валидатора (код удалён в v1.11.0),
  один бил по живой сети — юнит-сьют снова герметичен.
- Дрейф доков: `scan.md` (`steam_api_key` обязателен, vanity резолвится),
  README (`game_ids`/`game_ids_file` — объединение, не override), формат имени
  лог-файла в `logging_setup`/CLAUDE.md, стале-ссылки на удалённые cdp/dpapi.

## [1.11.0]

### Удаление мёртвого кода (legacy-чистка)

- **Мёртвое поддерево извлечения браузерных cookie удалено** (~600 строк).
  `get_web_cookies` давно работает по цепочке manual-cookie → web_refresh →
  JWT-CM → интерактивный playwright-login, а старая стратегия «тихого»
  извлечения (`_browser_cookies_silent`/`_browser_cookies`) не вызывалась
  ниоткуда. Удалены `app/cookies/{cdp,chrome,firefox,dpapi}.py` целиком и
  `_playwright_steam_cookies`; живые `_playwright_login` / `storage` /
  `web_refresh` не затронуты.
- **Осиротевшие символы удалены** (0 потребителей, grep по всему репо):
  `_save_shared_secret`, `_clear_credentials` и реэкспорт `_LEGACY_SESSION_FILE`
  (auth); `fetch_all_game_ids`, `fetch_badge_app_ids`, `read_steam_username`
  (steam); `_check_steam_process` (validator — дубль `check_steam_running`,
  который `validate()` не звал) вместе с его тестами.

### Исправлено

- **Зависимость `pycryptodome` → `pycryptodomex`**: код импортит namespace
  `Cryptodome.*` (RSA-логин `IAuthenticationService`), который предоставляет
  именно `pycryptodomex`; прежде это работало лишь транзитивно через
  `steam[client]`. Теперь реально-импортируемый пакет объявлен явно.
- Мёртвые `mypy`-overrides `win32con` / `win32gui` / `win32process` /
  `pywintypes` убраны — из pywin32 импортируется только `win32api`.

### Документация

- Устранён дрейф докстрингов и доков, ссылавшихся на удалённые модули
  (Store API, stats, categorize, card_store): `app/steam/__init__.py`,
  `app/cache.py`, `CLAUDE.md`,
  `docs/prompts/{scan,cards-farm,project-auditor}.md`.

## [1.10.1]

### Внутреннее

- **Диаг-скрипт `scripts/diag/dump_sam_window.py` перенесён в
  `docs/debug-sam-uia.md`** — техника дампа UIA-дерева окна SAM.Game (обход через
  `.children()`, обходя ловушку `child_window()` у `UIAWrapper`) сохранена как
  memo, а не запускаемый в цикле standalone-скрипт. Каталог `scripts/diag/`
  удалён; ссылки из `sam_status.py` и farm-промпта ведут на doc. Полная исходная
  версия — в истории git.

## [1.10.0]

### Удаление

- **Удалён advisory-каталог достижений целиком** — `scripts/categorize.py`,
  `app/catalog.py` и `app/steam/store_api.py` (+ их тесты). Каталог строил
  раскладку `with.txt`/`store_zero.txt`/`store_empty.txt` через Steam Store API.
  Для процесса «покрыть все игры во всех отношениях» он не нужен: влиял лишь на
  ПОРЯДОК обработки и подкидывал ненадёжные Store-догадки, а покрытие и так полное.

### Изменения

- **`achievements/farm.py`** перестал зависеть от каталога:
  - убран advisory-приоритет «игры с достижениями вперёд» — порядок теперь как в
    `all.txt` (покрытие не меняется, только порядок);
  - **`--retry-without` сужен до `without.txt`** (SAM-терминал «нет достижений»),
    без Store-советов `store_zero`/`store_empty` (Store всё равно ненадёжен).

## [1.9.4]

### Удаление

- **Удалены `scripts/stats.py` и `app/stats.py`** (сводка по библиотеке
  достижений: всего / с достижениями / без / разблокировано / ошибки / осталось,
  процент прогресса). Для процесса «покрыть все игры во всех отношениях»
  неактуально — сводка показывает в основном то, что НЕ удалось открыть. Убраны
  `LibraryStats`,
  `library_stats`, `format_library_stats` и `tests/unit/test_stats.py`;
  висячих импортов нет (символы больше нигде не использовались).

## [1.9.3]

### Удаление

- **Удалён аналитический `scripts/cards/scan.py`** — он лишь печатал таблицу игр
  с оставшимися card drops. Информация избыточна: цель всегда «выбить все
  карты», а `scripts/cards/farm.py` по умолчанию проходит все игры с дропами
  (очередь строит тот же `fetch_games_with_card_drops` из живого скрейпа
  badges). Мёртвого кода не появилось — `fetch_games_with_card_drops` остаётся
  используемым в `cards/farm.py`.

## [1.9.2]

### Удаление

- **Убран флаг `--reset` из `scripts/cards/farm.py`** — он был бесполезен:
  `cards/done.txt` write-only (очередь фарма строится из живого скрейпа badges,
  для пропуска игр он не читается), а повторно карты не выбиваются, так что
  чистить прогресс незачем. Вместе с флагом снят весь argparse-обвяз (у cards/
  farm больше нет CLI-флагов) и удалён ставший мёртвым `clear_card_progress`.
  `mark_card_done` и запись `cards/done.txt` сохранены.

## [1.9.1]

### Возможности

- **Флаг `--retry-done`** (`scripts/achievements/farm.py`) — перепрогнать через
  SAM ТОЛЬКО уже разблокированные игры (`unlocked.txt`), например когда в них
  добавили новые достижения после прошлого анлока. Селектор среза обобщён
  (`_select_retry_subset`): `--retry-without` и `--retry-done` можно комбинировать
  — берётся объединение подмножеств. При `--reset` оба игнорируются (reset и так
  гонит всю библиотеку).

### Исправления

- **Дрейф лога**: сообщение `_apply_resume_filter` ссылалось на удалённый в 1.9.0
  `--no-resume` → исправлено на актуальные `--reset`/`--retry-without`/`--retry-done`.

## [1.9.0]

### Возможности

- **`--retry-without` вместо `--no-resume`** (`scripts/achievements/farm.py`) —
  точечная перепроверка ТОЛЬКО игр без достижений: `without.txt` (SAM-терминал)
  ∪ Store-советы `store_zero.txt`/`store_empty.txt`. Нужно, чтобы прогнать через
  SAM игры, у которых достижения могли появиться позже (или SAM/Store ошиблись),
  не гоняя всю библиотеку. При одновременном `--reset` игнорируется (reset и так
  гонит всё) с предупреждением.
- **Самоочистка устаревших пометок**: при успешном анлоке игра снимается из
  `without` + Store-советов (`unmark_no_achievements` + `unmark_store_advisory`);
  при SAM-подтверждённом «нет достижений» снимается Store-совет (SAM авторитетнее).
  Срез `--retry-without` самосокращается, `stats` не задваивает игру. Новый
  атомарный примитив `_remove_id` (`app/id_file.py`, пустой файл удаляет).

### Удаление

- **Убран CLI-флаг `-v/--verbose`** из `farm`/`cards/farm`/`boost` — он лишь
  дублировал DEBUG в консоль, а лог-файл `logs/<category>/*.log` пишет DEBUG
  всегда. Параметр `verbose` у `setup_logging` сохранён (им пользуется
  диагностический `scripts/diag/dump_sam_window.py`).

## [1.8.1]

### Исправления

- **Честный отчёт farm: мягкая ошибка загрузки считается ошибкой** — прогон, где
  игры не загрузились (Steam offline / «global user» / протухшие куки), больше
  не рапортует ложное «✅ готово»: такие игры дают ⚠️ «с оговорками» и взводят
  аварийный стоп по серии. + тесты на маршрутизацию исхода одной игры.
- **playwright: видимое окно браузера закрывается на любом пути** в step-2 сбора
  куки — исключение в `new_context`/`goto` больше не оставляет окна висеть.

### Внутреннее

- Удалена осиротевшая `load_card_done_ids` (мёртвый API — единственным
  вызывающим был снятый в 1.8.0 GUI).
- Доки по итогам аудита сессии: CHANGELOG [1.8.0] F-1 называет верные
  (скалярные) поля; README-дерево дополнено (`notify.py`/`run_lock.py`/
  `validator.py`, `scripts/ci` + `scripts/diag`); auditor-промпт и докстринги
  очищены от устаревших GUI/card_store-ссылок.

## [1.8.0]

### Удаление

- **Удалён GUI** (`gui/`, `run.py`, зависимость `customtkinter`). Проект стал
  чисто консольным (`scripts/` + `app/`) — все возможности доступны через CLI.

### Исправления

- **Честный отчёт farm при прерывании** — `scripts/achievements/farm.py`
  больше не шлёт «✅ Готово» после Ctrl+C или аварийного стопа по серии ошибок;
  прерванный/аварийный прогон даёт честный ⚠️-статус (инвариант честного отчёта).
- **SAM: фокус окна перед кликами Unlock/Commit** — `set_focus` перед
  координатными кликами устраняет пропуск нажатий на неактивном окне (M1).
- **run-lock: устойчивость к TOCTOU** — compare-and-delete на устаревшем локе
  вместо гонки «проверка-затем-удаление» (M2).
- **id-файлы: атомарная запись** прогресса (+ `mkdir parents`) — обрыв на
  записи больше не повреждает прогресс-файлы (M3).
- **Steam CM: не стирать валидные креды** на транзиентном провале RSA-входа (M4).
- **SAM-апдейтер: сравнение версий тег-с-тегом**, не с PE-метаданными (M5).
- **notify: экранирование toast-строк** как PowerShell single-quoted литерал —
  устранена возможность инъекции через имя игры (Security).
- **win32: `restype=HANDLE`** для snapshot/OpenProcess на 64-битной Windows (critic).
- **cookies: гарантированное закрытие браузера** в `_playwright_login` даже при
  ошибке (critic).
- **config: `load_config` не падает на нечисловом скалярном числовом поле**
  (`load_timeout`, `playtime_concurrent_games`, `max_concurrent_games` и др.) —
  мусор пропускается с warning (F-1). (Списки `game_ids`/`exclude_ids` закрыты
  ранее в 1.7.1.)

### Внутреннее

- Handoff-промпты для сессий (`scan.py`, `achievements/farm.py`); чистка
  докового дрейфа (`ids.txt`→`all.txt`, докстринги `cache.py`), удаление
  устаревших planning-доков и stale GUI-ссылок в auditor-промпте.

## [1.7.1]

### Исправления (по финальному аудиту)

- **GUI: возвращена возможность очистить путь в настройках** (регрессия v1.7.0).
  При сохранении настроек очищенное в форме опциональное поле
  (`game_ids_file`/`steam_path`/`sam_game_exe_path`) не применялось — мерж
  сохранял старое значение, из-за чего farm/scan продолжали читать устаревший
  `game_ids_file`. Теперь пустое поле убирает ключ (сброс к дефолту), а ключи
  не из формы по-прежнему не теряются.
- **config: `load_config` не падает на нечисловом элементе** `game_ids`/
  `exclude_ids` — мусорный элемент пропускается с warning вместо сырого
  `ValueError`-трейсбека до валидации.

## [1.7.0]

### Playtime boost — надёжность и корректность (глубокий аудит, 42 агента)

- **Потеря игры (HIGH):** known-игра при разовом (часто транзиентном) провале
  подключения больше НЕ хоронится в `skip.txt` навсегда — skip только для
  unknown; known перепроверяет Steam API на следующем прогоне. Добавлены
  `clear_playtime_skip` и флаг `--retry-skips` для восстановления ранее
  отсеянных игр.
- **Свип процессов (HIGH):** любое исключение в `_boost_loop` (не только
  Ctrl+C) добивает активные + `kill_all_sam_games` через `finally` —
  `SAM.Game.exe` больше не сиротеют и не занимают global user.
- **Честный отчёт:** ✅-тост/Telegram только при полном прогоне без провалов;
  Ctrl+C, ошибка и наличие провалов → ⚠️ и честный текст (`_report_result`).
- **Пустой owned-games:** предупреждение при приватных Game details (все игры
  классифицируются unknown); `main` ловит `RuntimeError` из `_fetch_targets`.
- **Валидация конфига:** `playtime_idle_duration≥1`, `playtime_target_minutes≥1`,
  `launch_stagger≥0`; warning на не-список `exclude_ids`/`game_ids`.

### Run-lock и GUI

- **Run-lock:** берётся ДО деструктивного `--reset`/`--retry-skips` (`--list`
  теперь read-only). Формат `PID:create_time:name` отсекает PID-reuse, захват
  атомарен (`os.open O_EXCL`), release снимает ТОЛЬКО собственный лок.
- **Teardown:** второй Ctrl+C во время уборки не обрывает свип (bounded-retry).
- **GUI:** сохранение настроек мержит `config.yaml` — больше не затирает
  `playtime_concurrent_games`/`launch_stagger`/`playtime_target_minutes`/
  `telegram_*`/`game_ids`, которых нет в форме.

## [1.6.2]

### Устойчивость сети (hardening)

- `steam_api._api_get` и `validator._check_steam_api` ловят `ValueError`
  (`JSONDecodeError`/`UnicodeDecodeError`) при HTTP-200 с **не-JSON** телом
  (Cloudflare/captive-portal HTML) → `RuntimeError` / список ошибок вместо
  сырого трейсбека. `validate()` снова держит контракт «никогда не бросает».
  Завершает network-hardening класс. (#22)

## [1.6.1]

### Устойчивость сети и фарма карт (hardening)

- `card_checker._fetch_page` различает HTTP-коды: **429** → `_RateLimitError`
  с бэкоффом по `Retry-After`; **401/403** → `_AuthError` (истёкшие куки — НЕ
  ретраить, иначе 3 бессмысленные попытки маскируют auth-проблему под «связь»).
  Прочие HTTP/SSL — как раньше. (#18)
- `cards/farm.py`: при пачке `-1` (протухшая web-сессия) web-куки обновляются
  **неинтерактивно раз за батч** и остаток перечитывается — игры больше не
  метятся `unverified` из-за истёкшей сессии. (#18)
- Честный warning при упоре `_MAX_BADGE_PAGES`: явно предупреждает о возможной
  **НЕПОЛНОТЕ** списка игр (раньше выглядело штатным концом пагинации).
  Уточнены комментарии про широту `kill_all_sam_games` под run-lock. (#19)

## [1.6.0]

### Фарм карточек (cards)

- **Fast-mode цикл фарма**: пачка идлится → все окна убиваются РАЗОМ (коллапс
  in-game в ноль) → пауза-flush → перечитка остатка → выжившие в очередь и
  релонч. Устойчивая пагинация badges с ретраями на страницу (дёрганый SSL не
  теряет игры), stall-guard. Честный финальный отчёт: сдача по cookie-ошибке и
  застревания НЕ пишут `done` и не дают success-тост (тост «прерван» / «с
  оговорками» / чисто).

### Уведомления (notify)

- **Telegram-уведомления** рядом с локальным Windows-toast (opt-in по
  `telegram_*` в конфиге, best-effort).

### GUI

- **Остановка скрипта убивает всё дерево процессов** через Win32 Job Object
  (`KILL_ON_JOB_CLOSE`) — Stop/Esc/закрытие окна больше не оставляют сирот
  `SAM.Game.exe` (аккаунт не застревает in-game). Закрытие окна GUI сперва
  останавливает запущенные скрипты.

### Набивка playtime (boost)

- Отсев провалившихся запусков по **выживанию процесса** за весь idle, а не по
  транзиентному окну ошибки `SAM.Game.exe` (гонка). Страховка от сирот при
  Ctrl+C во время старта батча.

### Устойчивость сети (hardening)

- Сетевые запросы (`card_checker._fetch_page`, `steam_api._api_get`,
  `validator._check_steam_api`, `sam_downloader`) ловили только urllib
  `HTTPError`/`URLError`. При обрыве тела ответа `http.client.IncompleteRead`
  и при 302-редиректе `RemoteDisconnected` (подклассы `OSError`/`HTTPException`,
  но НЕ `URLError`) проходили мимо `except` и роняли весь прогон сырым
  исключением. Теперь оборачиваются в `RuntimeError` → штатный ретрай /
  чистый `sys.exit(1)` / `[CONFIG ERROR]`. (#13, #14, #15)

### Конфиг и валидация

- Числовые границы в pre-flight валидации (`max_concurrent_games`,
  `playtime_concurrent_games` ∈ [1, 20]; `card_check_interval` ≥ 1) — руками
  правленный `0`/отрицательный конфиг больше не даёт тихий no-op/busy-loop.

### Инфраструктура и документация

- `CLAUDE.md` (durable-правила проекта), `SECURITY.md`, `docs/prompts/`
  (playbook'и farm/boost/scan + аудитор). CI/pre-commit hardening: пин `ruff`,
  release-gate (VERSION==CHANGELOG==тег), UTF-8 stdout в `check_version`.
  Удалён мёртвый модуль `card_store`.

## [1.5.0]

### Каталог достижений (scan)

- Игры, по которым Store **ответил, но достижений нет** (`data:[]`/
  `success=false`), теперь кэшируются отдельно (`store_empty.txt`) и не
  опрашиваются каждый прогон. Транзиентные сбои сети остаются `unknown` и
  перезапрашиваются. `fetch_achievement_info` возвращает `AchievementInfo`
  с признаком `responded`, разделяя стабильное «нет данных» и сбой связи.
  `store_empty` — advisory, farm его не читает (игры не теряются).

### Достижения (farm)

- farm обрабатывает игры из каталога `with.txt` (Store подтвердил достижения)
  **первыми** — раньше доходит до гарантированно ненулевых игр. Состав
  очереди не меняется (advisory): остальные обрабатываются следом.

### Внутреннее

- Решение CM-логона «`bad_password` → RSA-путь vs пропуск CM» вынесено в
  чистый предикат `_password_failure_action` и покрыто тестами (поведение
  не изменилось).

## [1.4.0]

### Статистика и каталог (achievements)

- Новая команда `scripts/stats.py` — сводка по библиотеке достижений: всего /
  с достижениями / без / выбито / ошибки / осталось + процент прогресса.
- Новый advisory-каталог: `scripts/categorize.py` раскладывает библиотеку по
  данным Steam Store API на `with.txt` (Store подтвердил достижения) и
  `store_zero.txt` (Store сказал 0). Кэшируется и возобновляется (`--limit`,
  `--reset`). Переделка откатанного в v1.1.1 каталога — теперь **advisory**:
  влияет только на приоритет, никогда не приводит к пропуску игры. Терминально
  пометить «без достижений» по-прежнему вправе только SAM.
- `store_api.fetch_achievement_count`: отсутствие блока achievements →
  `None` (unknown), `0` только при явном `total == 0`. Больше не схлопывает
  «нет данных» в «0 достижений» (корень бага, из-за которого каталог откатили).

### Надёжность

- TOTP: `_compute_steam_totp` устойчив к повреждённому `shared_secret` —
  возвращает «нет авто-кода» вместо падения логона на `binascii.Error`.

### Внутреннее

- Покрыта тестами оркестрация современного входа в CM (`_rsa_jwt_login`,
  выбор кэш-файла по scope, мутация-проверка). `ensure_utf8_stdout` вынесен в
  `logging_setup` для print-скриптов (вывод `═`/кириллицы в cp1251-консоли).
- README: описаны новые команды, актуализированы структура и state-файлы.

## [1.3.0]

### Steam CM авторизация (scan)

- Полностью переработан вход в Steam CM для аккаунтов на современном Steam auth.
  Раньше `scan` бесконечно переспрашивал «Неверный пароль» и не получал ни одной
  игры по CM, хотя те же креды входят в веб/клиент Steam.
- Корень: legacy `client.login(пароль)` (ClientLogon) Steam отвергает для таких
  аккаунтов с `InvalidPassword` даже на ВЕРНОМ пароле. Теперь при этом
  выполняется современный путь `IAuthenticationService` (RSA + 2FA → JWT) и
  логон в CM по токену.
- Исправлена обработка Steam Guard 2FA: код мобильного аутентификатора
  (`DeviceCode`) больше не игнорируется; push-подтверждение
  (`DeviceConfirmation`) не запрашивает код, а ждёт подтверждения в приложении.
- CM-логон по JWT собирается как штатный `client.login` (ожидание шифрования
  канала + `steamid` в заголовке) и предъявляет `refresh_token` SteamClient-
  scope — короткий `access_token` CM отвергал с `AccessDenied`.
- После успешного входа `refresh_token` кэшируется (отдельно от веб-кук) →
  следующий скан входит в CM без пароля и 2FA.
- Прерван бесконечный ре-промпт пароля: реально неверный пароль завершается
  быстро, а не виснет.

### Auth (общее)

- Кэш `refresh_token` больше не удаляется при транзиентной ошибке сети
  (таймаут/нет ответа) и при пустом деривированном токене — стирается только
  при явном протухании/отзыве. Раньше кратковременный сбой сети заставлял
  вводить пароль/2FA каждый запуск.

## [1.2.0]

### Playtime (boost)

- По умолчанию набивает время во **всех** играх из `all.txt` (вся библиотека),
  а не только в тех, что отдаёт Steam Web API. Игры без данных API
  (free/демо/лицензии) идлятся вслепую один раз.
- Известные игры снимаются из набивки **по реальному** `playtime_forever`
  (перепроверяется каждый прогон), а не автоматически после одного идла;
  не добравшие target — повторяются. Невидимые API игры пишутся в
  `playtime/done.txt` (resume). Не подключившиеся → `playtime/skip.txt`.
- Скорость: отдельный `playtime_concurrent_games` (не общий с картами) и
  настраиваемый `launch_stagger` — главные рычаги пропускной способности.
- Флаг `--reset` сбрасывает прогресс набивки (`playtime/done.txt`).

### Achievements / Cards (farm)

- Заработали флаги `--retry-errors` / `--reset` / `--no-resume` (был добавлен
  argparse) — кнопки GUI «Retry errors» / «Reset» больше не пустышки;
  `cards/farm.py` получил рабочий `--reset`.

## [1.1.1]

### Откат

- Полностью откатан каталог достижений из 1.1.0. Код возвращён к состоянию
  1.0.0. Причина: классификация `scan` по Store API могла пометить игру с
  достижениями как «без достижений» (Store отдаёт пустой/отсутствующий блок
  achievements для playtest/демо/регион-лока/снятых с продажи), после чего
  farm пропускал её навсегда — нарушалась гарантия «всё неясное доезжает до
  SAM». Будет переделано с разделением «догадка scan» и «факт SAM».

## ~~[1.1.0]~~ (откатано в 1.1.1)

### Achievements (scan)
- `scan` после сбора `all.txt` классифицирует игры по наличию достижений
  через Steam Store API и раскладывает их на `with.txt` (есть достижения)
  и `without.txt` (нет) — без запуска SAM.
- Уже классифицированные игры (а также `unlocked.txt` от farm) повторно не
  опрашиваются; приложения, которых нет в Store (DLC, удалённые, серверы),
  не записываются и перепроверяются при следующем скане.
- Результат пишется по мере получения — прерывание безопасно, следующий
  запуск продолжает с нерасклассифицированных.

## [1.0.0]

Первый формальный релиз: стабильные farm / boost / cards + git-flow.

### Achievements (farm)
- Retry + нажатие **Refresh** при незагрузке статистики — игра больше не
  теряется в `error` из-за временной заминки.
- Быстрый поиск кнопки Refresh в `_MainToolStrip` (0.2 с вместо 4.6 с).
- Короткая перепроверка после Refresh; ранний выход для битых игр с пустым
  статусом (~8 с вместо 20 с).
- Откидывание в `error`, если статистика так и не загрузилась (без вечного
  RETRY).

### Cards (farm)
- Запуск `SAM.Game.exe` по очереди с паузой — устранена гонка за Steam
  global user.
- Окно браузера для входа в Steam открывается на весь экран.

### Playtime (boost)
- Поэтапный запуск батча с паузой между стартами и между батчами.
- Детект и skip игр, не подключающихся к Steam (playtest и пр.).

### Общее
- Защита от параллельного запуска farm / boost / cards (run-lock).
- Логирование: единый паттерн логгера, хелперы `SEPARATOR`/`centered`,
  централизованный UTF-8.
- `ruff format` по всей кодовой базе, pre-commit хуки (ruff + базовые).
- Документация git-flow процесса (`docs/gitflow.md`).
