# ── UI Automation ────────────────────────────────────────────────────────────
pywinauto>=0.6.8        # Windows UI Automation (SAM window control)

# ── Configuration ────────────────────────────────────────────────────────────
PyYAML>=6.0             # config.yml parsing

# ── Steam Protocol ───────────────────────────────────────────────────────────
steam>=1.4.4            # Steam CM protocol (login, app-list fetch)
gevent>=23.9.1          # Async I/O backend required by steam library
gevent-eventemitter~=2.1  # Required by steam[client]: SteamClient.count_listeners()

# ── System Utilities ─────────────────────────────────────────────────────────
psutil>=5.9             # Process enumeration (check if Steam is running)

# ── Security ─────────────────────────────────────────────────────────────────
keyring>=23.0           # Windows Credential Manager (store Steam password)
protobuf>=3.19,<4      # Protocol Buffers (used by steam library)
pycryptodome>=3.18      # RSA encryption for Steam IAuthenticationService login

# ── Browser Automation ────────────────────────────────────────
playwright>=1.40        # CDP cookie extraction (bypasses v20 Chrome encryption)
cryptography>=41.0      # AES-GCM decryption of Chrome v10 cookies (app/cookies/chrome.py)

# ── GUI ───────────────────────────────────────────────────────
customtkinter>=5.2.0    # Modern dark-themed UI (tkinter wrapper)

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0             # Unit test runner

# ── Dev Tooling ───────────────────────────────────────────────
ruff==0.15.16           # Linter + formatter (KEEP in sync with .pre-commit-config rev)
pre-commit>=4.0         # Git pre-commit hooks (ruff lint + format, mypy, basic checks)
mypy>=1.11              # Static type checker (scoped to app/, see pyproject.toml)
